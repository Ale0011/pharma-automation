/**
 * pharma-utils
 * ------------
 * General utility library used by the automation toolkit.
 * Exposes simple helper functions (placeholder for expansion).
 */

function calc(a, b) {
    return a + b;
}

module.exports = { calc };
